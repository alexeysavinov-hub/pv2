<?php
/**
 * ОБРАЗЕЦ настроек. На сервере скопируйте в inc/config.php и впишите пароли.
 * Сам inc/config.php в репозиторий не попадает (.gitignore).
 */

// Адрес сайта БЕЗ завершающего слэша — нужен для canonical и sitemap.
$SITE_URL = 'https://uitop.ru';

/**
 * ЗАКРЫТИЕ ОТ ПОИСКОВИКОВ (сайт в разработке)
 * true  — на всех страницах ставится <meta name="robots" content="noindex, nofollow">
 *         и заголовок X-Robots-Tag. Плюс robots.txt должен содержать Disallow: /
 * false — сайт открыт для индексации (не забудьте вернуть и robots.txt).
 */
$NOINDEX = true;

$SITE_NAME  = 'PCM — ваш эксперт по Porsche';
$SITE_PHONE = '+7 (495) 787-9-911';
$SITE_PHONE_HREF = '+74957879911';
$SITE_EMAIL = 'info@pc-moscow.ru';

/** Номер счётчика Яндекс.Метрики. Пусто — счётчик не подключается. */
$YM_ID = '';

/**
 * КУДА УХОДЯТ ЗАЯВКИ
 * По умолчанию — на $SITE_EMAIL. Здесь можно задать отдельный адрес для конкретной формы.
 * Ключи: callback — обратный звонок, service — запись на сервис,
 * finance — финансирование, car — вопрос по авто, contact — обращение с сайта.
 * Несколько адресов — через запятую.
 */
$FORM_EMAILS = [
  // 'callback' => 'evgeniy.sedler@pc-moscow.ru',
];
$SITE_ADDR  = '125445, Москва, Ленинградское шоссе, д. 71А, стр. 10';

/**
 * ФИДЫ MAXPOSTER — источник раздела «Авто в наличии».
 * Обновление: /import-cars.php?key=пароль_админки или по cron (см. эту страницу).
 * Машины, добавленные вручную в админке, при импорте сохраняются.
 */
$FEEDS = [
  'https://export.maxposter.ru/dealer-export/4026-23422.xml',
  'https://export.maxposter.ru/dealer-export/11816-87340.xml',
];

/**
 * ОТПРАВКА ПИСЕМ
 * Если оставить $SMTP_HOST пустым — письма уходят функцией mail() хостинга
 * (часто она не настроена и письма молча теряются).
 * Надёжный вариант — вписать данные почтового ящика:
 *   Яндекс 360:  smtp.yandex.ru, порт 465, secure = ssl
 *   Mail.ru:     smtp.mail.ru,   порт 465, secure = ssl
 * SMTP_USER — полный адрес ящика, SMTP_PASS — пароль приложения.
 * SMTP_FROM должен совпадать с ящиком, иначе сервер письмо не примет.
 */
$SMTP_HOST   = 'smtp.mail.ru';
$SMTP_PORT   = 465;
$SMTP_SECURE = 'ssl';   // ssl | tls
$SMTP_USER   = '';
$SMTP_PASS   = '';
$SMTP_FROM   = '';   // должен совпадать с ящиком, иначе сервер откажет

/**
 * ХРАНИЛИЩЕ ДАННЫХ
 * false — контент читается из JSON-файлов в папке data/. Туда же пишет админ-панель
 *         и импорт каталога, поэтому обновление файлов по FTP работает напрямую.
 *         Требование: папке data/ нужны права на запись (775).
 * true  — контент читается из базы MySQL (устаревший режим; файлы тогда
 *         игнорируются, пока в базе есть данные).
 */
$USE_DB = false;

$DB_HOST = 'localhost';
$DB_NAME = 'pcm';
$DB_USER = 'pcm';
$DB_PASS = '';

// Пароль входа в админ-панель и токен доступа к API.
$ADMIN_PASSWORD = 'СМЕНИТЕ_МЕНЯ';
$ADMIN_TOKEN    = 'СМЕНИТЕ_МЕНЯ_ДЛИННАЯ_СТРОКА';
